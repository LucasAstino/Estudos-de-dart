main(List<String> args) {
  num pi = 3;
  pi = 3.1415;//o num pode ser tantoint quanto double;
print(pi);
  int pi_arredondado  = pi.floor();//arredonda um valor;
  print(pi_arredondado);
  print(pi.isNegative);//informa seo numero e negativo;
  print(pi.isInfinite);//informa se o valor e infinito;
}